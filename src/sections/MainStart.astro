---
import ButtoWhatsapp from "../components/ButtoWhatsapp.astro";
---

<div id="imagen-wrapper">
  <div id="imagen"></div>
  <div class="content">
    <div id="texto">
      <h1>Mantén seguro lo que más quieres.</h1>
      <h2>
        Con TKUIDO descubre y compra las mejores pólizas de seguro al mejor
        precio.
      </h2>
      <p>
        Más del 80% de los trabajadores en Venezuela mantienen una relación
        laboral informal, y la mayoría de ellos no tienen ningún tipo de seguro.
      </p>
    </div>
    <div id="whatsapp-container">
      <ButtoWhatsapp
        text="Consulta en Linea"
        message="Hola! quiero más información de sus servicios"
      />
    </div>
  </div>
</div>

<style>
  #imagen-wrapper {
    position: relative;
    height: max-content;
    overflow: hidden;
    /** */
    margin-top: 50px;
    margin-left: calc(100vw - 98%);
    margin-right: calc(100vw - 98%);
    padding: 50px;
    border-radius: 15px;
  }
  #imagen {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: url("../../public/img/bgimg.png") no-repeat center fixed;
    background-size: cover;
    background-position: top;
  }
  /*#imagen {
    /*height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    margin-top: 50px;
    margin-left: calc(100vw - 98%);
    margin-right: calc(100vw - 98%);
    padding: 50px;
    background: url("../../public/img/bgimg.png") no-repeat center fixed; /*linear-gradient(rgba(5,7,12,0.15),rgba(5,7,12,0.15)),
    border-radius: 15px;
    background-size: cover;
    background-position: top;
  }*/
  .content {
    position: relative;
    z-index: 1;
  }
  #texto {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    width: 50%;
    text-align: start;
  }
  #whatsapp-container {
    margin-top: 40px;
  }
  h1 {
    font-size: 35px;
  }
  h2 {
    font-size: 20px;
  }
  p {
    font-size: 16px;
  }
  h1,
  h2,
  p {
    margin-bottom: 20px;
  }

  @media (max-width: 769px) {
    #imagen-wrapper {
      margin-left: calc(100vw - 98vw);
      margin-right: calc(100vw - 98vw);
      padding: 25px;
    }
    #imagen {
      background: url("../../public/img/bgimg.png") no-repeat center;
      background-position: 75%;
      background-size: cover;
      filter: brightness(50%);
    }
    .content {
      display: flex;
      flex-direction: column;
    }
    .content:last-child{
      column-gap: 20px;
    }
    #texto {
      color: #fff;
      text-align: center;
      width: fit-content;
    }
    #whatsapp-container {
      margin: auto;
    }
  }
</style>
